import 'dart:html';

import 'package:flutter/material.dart';

class ChartBar extends StatelessWidget {
  final String label;
  final double value;

  ChartBar(this.label, this.value);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
